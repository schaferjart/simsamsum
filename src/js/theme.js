/**
 * Theme management functionality.
 * Note: Most theme functionality is now handled in ui.js
 * This file exists for compatibility.
 */

export class ThemeManager {
    constructor() {
        // Theme management is now handled in ui.js
        console.log('ThemeManager initialized (legacy compatibility)');
    }
}

// Default export for backwards compatibility
export default ThemeManager;
